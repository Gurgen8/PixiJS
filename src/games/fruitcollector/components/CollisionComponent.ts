export class CollisionComponent {
  public hitWidth: number;
  public hitHeight: number;
  constructor(hitWidth: number, hitHeight: number) {
    this.hitWidth = hitWidth;
    this.hitHeight = hitHeight;
  }
}
